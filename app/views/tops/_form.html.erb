<p id="notice"><%= notice %></p>

<%= form_for(@top, url: choose_new_or_edit) do |f| %>

  <% if @top.errors.any? %>
    <div id="error_explanation">
      <h2><%= @top.errors.count %>件のエラーがあります。</h2>
      <ul>
      <% @top.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%#<%= f.label :content %>
  <br>
  <%= f.text_field :content, class: 'form-control' %>
  <br>
  <%= f.submit 'つぶやく', class: 'btn btn-primary btn-block' %>
<% end %>
<br>
<%= link_to "つぶやき一覧画面にもどる", tops_path %>